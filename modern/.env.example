# Copy to Vercel → Project → Settings → Environment Variables

# Required for production (Vercel = frontend only; Python API hosted elsewhere)
# Example: https://your-app.onrender.com  (no trailing slash)
VITE_API_URL=
